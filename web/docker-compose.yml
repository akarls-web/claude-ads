services:
  web:
    build: .
    ports:
      - "3100:3100"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3100/api/trpc"]
      interval: 30s
      timeout: 5s
      retries: 3
